import server from "server/server.config.js";
import mongo from "mongo/mongo.config.js";
import auth from "auth/auth.config.js";
import passport from "passport/passport.config.js";

export default config = { server, mongo, auth, passport };
